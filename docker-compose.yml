version: "3.9"

x-backend-environment: &backend-environment
  DJANGO_SETTINGS_MODULE: settings.settings.prod
  CELERY_BROKER_URL: redis://redis:6379/0
  CELERY_RESULT_BACKEND: redis://redis:6379/0

x-backend-base: &backend-base
  build:
    context: .
    dockerfile: Dockerfile
  volumes:
    - ./:/mm
    - ./logs:/mm/logs/
    - ./scripts:/mm/scripts/
    - ./src/static:/mm/src/static/
    - ./src/media:/mm/src/media/

x-backend: &backend
  image: app:django
  <<: *backend-base
  environment:
    <<: *backend-environment

services:

  # Redis
  redis:
    image: redis:alpine
    container_name: mm_redis
    ports:
      - 6380:6379

  # Database Postgres
  db:
    image: postgres:15.4
    restart: always
    command:
      - "postgres"
      - "-c"
      - "max_connections=50"
      # ... дополнительные настройки PostgreSQL ...
    volumes:
      - ./data/db:/var/lib/postgresql/data
    environment:
      - POSTGRES_PASSWORD=postgres
      - POSTGRES_USER=postgres
      - POSTGRES_DB=mega_mailer
    ports:
      - ${POSTGRES_PORT:-5433}:5432
    container_name: mm_postgres
    healthcheck:
      test: [ "CMD-SHELL", "pg_isready -U postgres -d mega_mailer" ]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 10s
    deploy:
      resources:
        limits:
          cpus: '1'
          memory: 4G

  # Django Gunicorn ASGI App
  wsgi-server:
    <<: *backend
    ports:
      - 8000:8000  # Пример порта для Gunicorn (можно использовать любой свободный)
    links:
      - db
      - redis
    container_name: mm_wsgi
    command: sh -c "./scripts/wait-for-it.sh db:5432 -- gunicorn --bind 0.0.0.0:8000 --workers 4 settings.asgi:application"
    depends_on:
      db:
        condition: service_healthy

  # Celery, Flower, Nginx и другие сервисы остаются без изменений

  # Nginx
  nginx:
    build:
      context: ./nginx
      dockerfile: Dockerfile
    container_name: mm_nginx
    restart: always
    ports:
      - 80:80
    volumes:
      - ./nginx/default.conf:/etc/nginx/conf.d/default.conf
    depends_on:
      - wsgi-server
    networks:
      - redis-django

networks:
  redis-django:
    driver: bridge
